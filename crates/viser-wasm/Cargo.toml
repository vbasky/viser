[package]
name = "viser-wasm"
version = "0.10.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "WASM bindings for viser no-reference quality metrics"
keywords = ["wasm", "video", "quality", "niqe", "brisque"]
categories = ["wasm", "multimedia::video"]

[lib]
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true

[dependencies]
wasm-bindgen = "0.2"
viser-quality = { path = "../viser-quality", version = "0.10.0" }